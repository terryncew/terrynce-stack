{
  "name": "terrynce-stack",
  "private": true,
  "type": "module",
  "engines": { "node": ">=18.0.0" },
  "scripts": {
    "emit": "node examples/predict_spy.mjs",
    "api": "uvicorn server.main:app --port 8000 --reload"
  }
}
